# ArchiveGuard evaluation project

This small library computes safe destination paths for untrusted archive
members and writes extracted data beneath a caller-supplied root directory.

Run tests with:

    python3 -m unittest discover -s tests -v
